# EppTools для Java — довідковий посібник

Версія **1.1.2**.

```xml
<dependency>
  <groupId>io.github.epptools</groupId>
  <artifactId>epptools-sdk</artifactId>
  <version>1.1.2</version>
</dependency>
```

```groovy
implementation 'io.github.epptools:epptools-sdk:1.1.2'
```

## Для кого це

Ви інтегруєте білінгову систему або систему надання послуг із доменним реєстром, який говорить EPP,
і реєстр видав вам clID та пароль.

Ця бібліотека — **клієнт EPP**, і не клієнт якогось одного реєстру. Вона відкриває TLS-сокет до
реєстру — за домовленістю на порт 700, — пише кадри RFC 5734 і читає відповіді. Усе в цьому
посібнику — це протокол передачі **RFC 5730–5734** з об'єктом Java перед ним: сесія, привітання,
команди, відповіді, коди відповіді, об'єкти, розширення та черга poll.

Там, де реєстр додає до протоколу щось своє, бібліотека читає, що саме, з `<greeting>` від сервера,
а не дізнається це на етапі збирання, — тож налаштовувати тут під конкретний реєстр нічого, а
приклади працюють там, куди ви їх спрямуєте.

Посібник виходить із того, що ви вмієте писати Java 8 або новішої версії і що EPP для вас новий.
Там, де метод відповідає конкретній команді EPP або конкретному RFC, сторінка називає їх, щоб ви
могли читати власний посібник реєстру поруч із цим і впізнавати те, що бачите.

Дві звички, які варто засвоїти, перш ніж писати будь-що, що коштує грошей:

- **Дати та гроші повертаються рядками самого реєстру**, ніколи не як `Instant` чи `double`. Реєстр
  вирішує, на який календарний день припаде продовження, а `0.1 + 0.2` не дорівнює `0.3` у двійковій
  рухомій комі.
- **Команда, відповідь на яку ніхто не читає, — це половина інтеграції.** Кожна сторінка показує,
  як читають відповідь, а не лише як надсилають команду.

## Дванадцять сторінок, у порядку читання

| Сторінка | Про що вона |
|---|---|
| [Зміст](README.md) | ця сторінка: що містить посібник, у якому порядку і для кого |
| [Швидкий старт](quickstart.md) | встановлення, підключення, вхід, одна справжня команда, вихід — одна повна програма, розібрана рядок за рядком |
| [Сесія](session.md) | `Config` поле за полем, перевірка TLS і невдалі рукостискання, connect/hello/login/logout, зміна пароля, події безпеки RFC 8807, журналювання із замаскованими секретами |
| [Команди](commands.md) | поверхня команд як ціле: що повертає команда, ідентифікатори транзакцій клієнта, перемикач `throwOnFailure` і побудова власного кадру для всього, що не охоплено |
| [Домени](domains.md) | кожен доменний метод (RFC 5731) — check, info, create, update, renew, restore, delete, transfer — разом із DNSSEC та ліцензією `.ua` |
| [Контакти](contacts.md) | кожен контактний метод (RFC 5733), зокрема ідентифікатори, згенеровані реєстром, і налаштування розкриття |
| [Хости](hosts.md) | кожен метод для серверів імен (RFC 5732), glue-адреси та примусове видалення |
| [Poll](poll.md) | черга повідомлень: запит, підтвердження, спорожнення і що несуть сповіщення |
| [Баланс](balance.md) | баланс облікового запису, а також ціни та комісії (RFC 8748): як запитати, як обмежити, як прочитати |
| [Відповіді](responses.md) | кожен аксесор `Response`, згрупований за тим, на що він відповідає |
| [Білдери](builders.md) | плавні білдери: кожен крок кожного білдера, правило накопичення і правило одного надсилання |
| [Помилки](errors.md) | таксономія винятків, коди відповіді, правила повторів і робота з невідомим результатом |

Почніть зі [Швидкого старту](quickstart.md). Прочитайте [Сесію](session.md) та
[Помилки](errors.md), перш ніж надсилати першу платну команду.

## Вимоги

Java 8 або новіша. Опублікований артефакт скомпільовано з `--release 8`, тож той самий jar працює на
8, 11, 17, 21 і кожному пізнішому випуску. TLS бере з `javax.net.ssl`, DOM — з `javax.xml`; обидва
входять до JDK, і більше бібліотека не залежить ні від чого — ні від XML-бібліотеки, ні від фасаду
журналювання, ні від HTTP-клієнта.

## Підтримка

Питання про бібліотеку, кадр, який реєстр відхилив, або помилка: **https://github.com/epptools/java-sdk/issues**.

Повідомляючи про проблему, наведіть **svTRID** з відповіді та **clTRID**, який надіслав ваш клієнт —
разом вони визначають точну транзакцію в журналах реєстру, і саме це дозволяє відповісти на
звернення без зайвого кола листування. Надішліть і кадри, якщо можете, але спершу вилучіть `<pw>`,
`<newPW>` та `<authInfo>`: це діючі облікові дані, і бібліотека маскує їх у власних журналах з тієї
самої причини.

Наведіть також версію бібліотеки — `com.epptools.sdk.Version.VERSION`. Це той самий рядок, який цей
клієнт повідомляє серверу під час входу (RFC 8807 `<loginSec:app>`), тож версія у вашому зверненні та
версія, яку побачив реєстр, — це одна версія:

```java
System.out.println(com.epptools.sdk.Version.VERSION);   // "1.1.2"
```

Питання про обліковий запис, рахунки та реєстрацію — до вашого менеджера в реєстрі, не сюди: ця
адреса стосується клієнтської бібліотеки.
